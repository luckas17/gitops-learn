apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: httpbin-gw
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        protocol: HTTP
      hosts:
        - "*"